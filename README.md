# Hyprconfig

Hyprconfig (sorta tempary name) is a small little cli app that makes it easier to mange Hyprland configs.

## How to install

Run this:
```bash
pip install git+https://github.com/AceandGaming/hyprconfig.git
```

Then use `config` in the cli.
Use `config --help` for more info!
